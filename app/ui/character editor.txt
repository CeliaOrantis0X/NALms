2026/1/2
取消：
CANCEL（直接关闭编辑界面）

新增：
1. CLEAR（清除所有已输入或导入的数据）
2. Dirty State：修改过才允许 CLEAR / SAVE
3. 关闭窗口时 Dirty 提示（是否放弃修改）
4. Auto-save Draft（每 N 秒）, 自动保存草稿, 确认后清楚内存中的草稿

修改：
1. CONFIRM（确认修改并关闭编辑界面）-> 确认修改，弹窗提示，但不退出界面

2026/1/3
根据代码，给出具体建议和实现方法，逐步完成UI布局调整：
1. 顶部工具栏：导入、保存、确认、重置。状态信息位于工具栏中间位置。
2. 编辑区分为上下两个等比部分。
3. 上半区分为左右两个部分。左侧上方为背景填充的形式导入的人物插图，左侧下方（1/3）为人物评级数据（魅力、战力、敏捷、力量、智力、感知、领导）；
右侧上方（2/3）为人物基本属性（姓名、性别、年龄、身高等），右侧下方（1/3）为杂项（来源、羁绊、标签）。
4. 下半区为可编辑的文本区域，在输入区域上方增加并排设置的按钮栏，包括：外貌、简介、性格、能力。点击按钮输入框会自动切换到相应的输入区域。
5. 底部：故事导入。
以下是当前代码：